# 🎯 Healthcare智能预约与健康监测系统 - 完整实施计划

## 📋 项目概览

**项目名称**: Healthcare智能预约与健康监测系统  
**课程代码**: CT071-3-3-DDAC  
**项目类型**: Web Application (前后端分离架构)  
**开发周期**: 8周 (Week 6 - Week 14)  
**团队规模**: 1-4人  
**预算**: RM 0 (完全免费方案)

---

## 🏥 行业选择与问题陈述

### **选定行业**: Healthcare Industry (医疗保健行业)

### **行业当前挑战**:

1. **预约效率低下**: 传统电话预约耗时且容易出错
2. **医疗资源分配不均**: 患者难以找到合适的专科医生
3. **病历管理混乱**: 纸质病历容易丢失，数据安全性低
4. **缺乏健康监测**: 慢性病患者缺乏持续的健康数据追踪
5. **医疗咨询门槛高**: 轻微症状患者不知道该看哪个科室

### **我们的解决方案**:

开发一个云端智能医疗预约系统，整合AI症状分析、区块链病历存储、实时健康监测等先进技术，提供高效、安全、智能的医疗服务平台。

---

## 🎯 课业要求对照表

| 课业要求               | 我们的实现                       | 分值   | 状态  |
| ------------------ | --------------------------- | ---- | --- |
| **基础要求**           |                             |      |     |
| 使用ASP.NET开发        | ASP.NET Core 8.0 Web API    | 必须   | ✅   |
| 部署到AWS云环境          | EC2 + RDS + S3 + CloudFront | 必须   | ✅   |
| 使用AWS监控工具          | CloudWatch + X-Ray          | 必须   | ✅   |
| 云计算服务整合            | 7个AWS服务                     | 必须   | ✅   |
| **Task #1 (30分)**  |                             |      |     |
| 前端开发               | React 18 + TypeScript       | 10分  | ✅   |
| 后端开发               | ASP.NET Core Web API        | 10分  | ✅   |
| 云数据库               | AWS RDS PostgreSQL          | 10分  | ✅   |
| **Task #2 (20分)**  |                             |      |     |
| 云服务整合              | S3, Lambda, DynamoDB        | 10分  | ✅   |
| 监控工具               | CloudWatch Dashboard        | 10分  | ✅   |
| **Distinction加分项** |                             |      |     |
| AI整合               | Google Gemini API (症状分析)    | +20分 | ✅   |
| Blockchain         | 简易区块链实现                     | +15分 | ✅   |
| NoSQL数据库           | AWS DynamoDB                | +10分 | ✅   |
| Serverless架构       | AWS Lambda函数                | +10分 | ✅   |
| Vector Database    | PostgreSQL pgvector         | +15分 | ✅   |
| 高级安全措施             | JWT + 数据加密                  | +10分 | ✅   |

**预估总分**: 50 (基础) + 80 (加分) = **100+分**  
**目标等级**: **Distinction (80%+)**

---

## 🛠️ 技术栈详细规格

### **1. Frontend (前端) - Web Application**

```
核心框架:
├── React 18.2+ (最新稳定版)
├── TypeScript 5.0+ (类型安全)
└── Vite (构建工具，比create-react-app更快)

UI框架:
├── Material-UI (MUI) v5
│   └── 预制组件，加速开发
└── Tailwind CSS (可选，用于自定义样式)

状态管理:
├── React Context API (全局状态)
└── React Query (服务器状态缓存)

路由:
└── React Router v6 (单页应用路由)

HTTP客户端:
└── Axios (API调用)

图表库:
└── Recharts (数据可视化)

其他工具:
├── React-Toastify (通知提示)
├── date-fns (日期处理)
└── Formik + Yup (表单验证)
```

### **2. Backend (后端) - API服务器**

```
核心框架:
└── ASP.NET Core 8.0 Web API
    ├── 最新LTS版本
    ├── 原生支持RESTful API
    └── 高性能跨平台

ORM (对象关系映射):
└── Entity Framework Core 8.0
    ├── Code-First方式
    ├── 自动迁移管理
    └── LINQ查询

认证授权:
├── JWT (JSON Web Tokens)
├── BCrypt.Net (密码加密)
└── Microsoft.AspNetCore.Authentication.JwtBearer

API文档:
└── Swagger/OpenAPI (自动生成API文档)

依赖注入:
└── ASP.NET Core 内置DI容器
```

### **3. 数据库架构**

#### **主数据库: AWS RDS PostgreSQL 15**

```
用途: 关系型数据持久化
免费额度: 
├── db.t3.micro实例
├── 20GB存储
└── 750小时/月运行时间

存储内容:
├── 用户信息 (Users表)
├── 医生信息 (Doctors表)
├── 预约记录 (Appointments表)
├── 病历记录 (MedicalRecords表)
├── 处方信息 (Prescriptions表)
└── 症状向量 (SymptomVectors表 - 用于AI)

扩展:
└── pgvector (向量数据库扩展)
    └── 用于AI语义搜索
```

#### **辅助数据库: AWS DynamoDB**

```
用途: NoSQL高速数据存储
免费额度:
├── 25GB存储 (永久免费)
├── 25个读写容量单位
└── 25GB数据传输

存储内容:
├── 健康监测数据 (心率、血压、血糖)
│   └── 分区键: PatientId, 排序键: Timestamp
├── Session会话数据
│   └── TTL自动过期
└── AI分析缓存
    └── 避免重复调用AI API
```

### **4. 云存储服务**

```
AWS S3 (Simple Storage Service):
├── 前端静态文件托管
│   └── React构建产物 (HTML/CSS/JS)
├── 医疗文档存储
│   ├── 检查报告 (PDF, JPG)
│   ├── 处方单
│   └── X光片/CT扫描图
├── 免费额度: 5GB存储 + 20,000 GET请求
└── 生命周期管理 (30天后转存到低成本S3 Glacier)

AWS CloudFront (CDN):
├── 前端加速分发
├── HTTPS自动证书
├── 免费额度: 1TB数据传输/月
└── 缓存策略: 静态资源缓存24小时
```

### **5. 计算服务**

```
AWS EC2 (后端托管):
├── 实例类型: t2.micro (免费)
├── 操作系统: Ubuntu 22.04 LTS
├── 配置: 1 vCPU, 1GB RAM
├── 免费额度: 750小时/月
└── 安全组配置:
    ├── 22端口 (SSH)
    ├── 80端口 (HTTP)
    ├── 443端口 (HTTPS)
    └── 5000端口 (ASP.NET默认)

AWS Lambda (Serverless函数):
├── 预约提醒自动化
│   └── 每天晚上8点执行
├── 数据备份任务
│   └── 每周日凌晨2点执行
├── 免费额度: 100万次请求/月
└── 运行时: .NET 8
```

### **6. AI与机器学习服务**

```
Google Gemini API (免费方案):
├── 模型: gemini-pro (文本) + gemini-pro-vision (图像)
├── 免费额度: 60次/分钟
├── 功能实现:
│   ├── 症状分析助手
│   │   └── 输入症状 → 推荐科室 + 注意事项
│   ├── 药物识别
│   │   └── 上传药物图片 → 识别名称和用途
│   └── 健康报告解读
│       └── 上传检验报告 → AI解读异常指标
└── 备用方案: OpenAI API ($5免费额度)

PostgreSQL pgvector扩展:
├── 向量维度: 1536 (OpenAI embedding标准)
├── 索引类型: IVFFlat (快速近似搜索)
├── 功能: 相似症状搜索
└── 流程:
    ├── 将症状描述转为向量
    ├── 搜索历史相似病例
    └── 辅助AI诊断建议
```

### **7. 区块链实现**

```
方案: 自建简易区块链 (不使用AWS Managed Blockchain)
原因: 
├── AWS Managed Blockchain无免费额度
└── 自建方案更适合演示和学习

技术实现:
├── 语言: C# (集成在ASP.NET后端)
├── 哈希算法: SHA-256
├── 区块结构:
│   ├── Index (区块索引)
│   ├── Timestamp (时间戳)
│   ├── Data (病历数据JSON)
│   ├── PreviousHash (前一区块哈希)
│   └── Hash (当前区块哈希)
└── 存储: PostgreSQL专用表 (Blockchain表)

应用场景:
├── 病历记录防篡改
├── 处方开具记录上链
├── 患者授权记录
└── 验证链完整性功能
```

### **8. 监控与分析服务**

```
AWS CloudWatch:
├── 指标监控
│   ├── EC2: CPU使用率、内存、磁盘I/O
│   ├── RDS: 数据库连接数、查询延迟
│   ├── API: 请求响应时间、错误率
│   └── 自定义指标: 预约创建数、AI调用次数
├── 日志管理
│   └── CloudWatch Logs (应用日志集中存储)
├── Dashboard
│   └── 实时监控仪表盘 (4-6个图表)
└── 告警
    └── CPU > 80% 发送SNS通知

AWS X-Ray (可选):
├── 分布式追踪
├── 性能瓶颈分析
└── API调用链可视化

CloudWatch Insights:
└── 日志查询和分析 (SQL-like语法)
```

---

## 🏗️ 系统架构设计

### **整体架构图**

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户浏览器                                 │
│                  https://healthcare-app.com                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓ HTTPS
┌─────────────────────────────────────────────────────────────────┐
│              AWS CloudFront (CDN全球加速)                         │
│              ├─ 缓存静态资源 (CSS/JS/图片)                        │
│              └─ 自动HTTPS证书                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│        AWS S3 Bucket (前端静态托管)                               │
│        └─ React Build输出 (HTML/CSS/JS)                          │
└─────────────────────────────────────────────────────────────────┘

                              ↓ REST API (JSON)
                              ↓ https://api.healthcare-app.com

┌─────────────────────────────────────────────────────────────────┐
│       AWS Application Load Balancer (可选)                       │
│       └─ HTTPS终结 + 负载均衡                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              AWS EC2 (t2.micro)                                  │
│              ASP.NET Core 8.0 Web API                            │
│              ├─ Controllers (RESTful API)                        │
│              ├─ Services (业务逻辑层)                             │
│              │   ├─ AIService (Gemini API调用)                   │
│              │   ├─ BlockchainService (区块链)                   │
│              │   ├─ S3Service (文件上传)                         │
│              │   └─ DynamoDBService (NoSQL操作)                  │
│              └─ Entity Framework Core (ORM)                      │
└─────────────────────────────────────────────────────────────────┘
       ↓                    ↓                    ↓
┌──────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ AWS RDS      │  │  AWS DynamoDB    │  │   AWS S3         │
│ PostgreSQL   │  │  (NoSQL)         │  │   (文件存储)      │
│ + pgvector   │  │  ├─HealthMetrics │  │   ├─检查报告      │
│              │  │  ├─Sessions      │  │   ├─处方单        │
│ ├─Users      │  │  └─AI Cache      │  │   └─医疗影像      │
│ ├─Doctors    │  │                  │  │                  │
│ ├─Appointments│ │                  │  │                  │
│ ├─MedicalRec  │  │                  │  │                  │
│ └─Blockchain  │  │                  │  │                  │
└──────────────┘  └──────────────────┘  └──────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              AWS Lambda (Serverless函数)                          │
│              ├─ 预约提醒函数 (每日20:00触发)                       │
│              └─ 数据备份函数 (每周日02:00触发)                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              AWS CloudWatch (监控与日志)                          │
│              ├─ Metrics (性能指标)                                │
│              ├─ Logs (应用日志)                                   │
│              ├─ Dashboard (可视化仪表盘)                          │
│              └─ Alarms (告警通知)                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              外部API服务                                           │
│              └─ Google Gemini API (AI分析)                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 数据库设计 (详细表结构)

### **PostgreSQL关系型数据库**

#### **表1: Users (用户表)**

```
字段名              数据类型          约束            说明
UserId            SERIAL          PRIMARY KEY    自增主键
Email             VARCHAR(100)    UNIQUE, NOT NULL  邮箱
PasswordHash      VARCHAR(255)    NOT NULL       BCrypt加密密码
FullName          VARCHAR(100)    NOT NULL       全名
PhoneNumber       VARCHAR(20)                    电话号码
UserRole          VARCHAR(20)     NOT NULL       角色: Patient/Doctor/Admin
ProfileImage      TEXT                           头像URL (S3链接)
DateOfBirth       DATE                           出生日期
Gender            VARCHAR(10)                    性别
Address           TEXT                           地址
CreatedAt         TIMESTAMP       DEFAULT NOW()  创建时间
UpdatedAt         TIMESTAMP                      更新时间
IsActive          BOOLEAN         DEFAULT TRUE   账户状态

索引:
├── UNIQUE INDEX idx_email ON Users(Email)
└── INDEX idx_role ON Users(UserRole)
```

#### **表2: Doctors (医生表)**

```
字段名              数据类型          约束            说明
DoctorId          SERIAL          PRIMARY KEY    自增主键
UserId            INTEGER         FOREIGN KEY    关联Users表
Specialization    VARCHAR(100)    NOT NULL       专科: 心脏科/儿科/皮肤科等
LicenseNumber     VARCHAR(50)     UNIQUE         执业证号
YearsOfExperience INTEGER                        从业年限
Biography         TEXT                           个人简介
ConsultationFee   DECIMAL(10,2)   NOT NULL       咨询费用 (RM)
ClinicAddress     TEXT                           诊所地址
WorkingHours      JSONB                          工作时间 (JSON格式)
Rating            DECIMAL(3,2)    DEFAULT 0.00   评分 (0-5)
TotalReviews      INTEGER         DEFAULT 0      评价数量
IsAvailable       BOOLEAN         DEFAULT TRUE   是否接受预约

外键:
└── FOREIGN KEY (UserId) REFERENCES Users(UserId) ON DELETE CASCADE
```

#### **表3: Appointments (预约表)**

```
字段名              数据类型          约束            说明
AppointmentId     SERIAL          PRIMARY KEY    自增主键
PatientId         INTEGER         FOREIGN KEY    患者ID
DoctorId          INTEGER         FOREIGN KEY    医生ID
AppointmentDate   TIMESTAMP       NOT NULL       预约日期时间
Duration          INTEGER         DEFAULT 30     预约时长(分钟)
Status            VARCHAR(20)     NOT NULL       状态: Pending/Confirmed/Completed/Cancelled
Symptoms          TEXT                           症状描述
AIAnalysisResult  TEXT                           AI分析结果
Notes             TEXT                           备注
CancellationReason TEXT                          取消原因
CreatedAt         TIMESTAMP       DEFAULT NOW()  创建时间
UpdatedAt         TIMESTAMP                      更新时间

外键:
├── FOREIGN KEY (PatientId) REFERENCES Users(UserId)
└── FOREIGN KEY (DoctorId) REFERENCES Doctors(DoctorId)

索引:
├── INDEX idx_patient ON Appointments(PatientId)
├── INDEX idx_doctor ON Appointments(DoctorId)
├── INDEX idx_date ON Appointments(AppointmentDate)
└── INDEX idx_status ON Appointments(Status)
```

#### **表4: MedicalRecords (病历表)**

```
字段名              数据类型          约束            说明
RecordId          SERIAL          PRIMARY KEY    自增主键
AppointmentId     INTEGER         FOREIGN KEY    关联预约ID
PatientId         INTEGER         FOREIGN KEY    患者ID
DoctorId          INTEGER         FOREIGN KEY    医生ID
Diagnosis         TEXT            NOT NULL       诊断结果
Symptoms          TEXT                           症状记录
VitalSigns        JSONB                          生命体征 (JSON格式)
LabResults        JSONB                          检验结果
TreatmentPlan     TEXT                           治疗方案
Notes             TEXT                           医生备注
Attachments       JSONB                          附件链接 (S3 URLs)
BlockchainHash    VARCHAR(64)                    区块链哈希值
CreatedAt         TIMESTAMP       DEFAULT NOW()  创建时间
UpdatedAt         TIMESTAMP                      更新时间

外键:
├── FOREIGN KEY (AppointmentId) REFERENCES Appointments(AppointmentId)
├── FOREIGN KEY (PatientId) REFERENCES Users(UserId)
└── FOREIGN KEY (DoctorId) REFERENCES Doctors(DoctorId)
```

#### **表5: Prescriptions (处方表)**

```
字段名              数据类型          约束            说明
PrescriptionId    SERIAL          PRIMARY KEY    自增主键
RecordId          INTEGER         FOREIGN KEY    病历ID
PatientId         INTEGER         FOREIGN KEY    患者ID
DoctorId          INTEGER         FOREIGN KEY    医生ID
Medications       JSONB           NOT NULL       药物列表 (JSON数组)
Dosage            TEXT                           用量说明
Instructions      TEXT                           服用说明
Duration          INTEGER                        疗程(天)
RefillsAllowed    INTEGER         DEFAULT 0      允许续方次数
IssueDate         TIMESTAMP       DEFAULT NOW()  开具日期
ExpiryDate        TIMESTAMP                      过期日期
BlockchainHash    VARCHAR(64)                    区块链哈希值
Status            VARCHAR(20)     DEFAULT 'Active' 状态

外键:
├── FOREIGN KEY (RecordId) REFERENCES MedicalRecords(RecordId)
├── FOREIGN KEY (PatientId) REFERENCES Users(UserId)
└── FOREIGN KEY (DoctorId) REFERENCES Doctors(DoctorId)
```

#### **表6: SymptomVectors (症状向量表 - 用于AI)**

```
字段名              数据类型          约束            说明
VectorId          SERIAL          PRIMARY KEY    自增主键
SymptomText       TEXT            NOT NULL       症状描述
Embedding         VECTOR(1536)    NOT NULL       向量嵌入 (pgvector类型)
RecommendedSpecialty VARCHAR(100)                推荐科室
Diagnosis         TEXT                           常见诊断
Severity          VARCHAR(20)                    严重程度: Mild/Moderate/Severe
CreatedAt         TIMESTAMP       DEFAULT NOW()  创建时间

索引:
└── CREATE INDEX ON SymptomVectors USING ivfflat (Embedding vector_cosine_ops)
   └── 用于快速向量相似度搜索
```

#### **表7: Blockchain (区块链表)**

```
字段名              数据类型          约束            说明
BlockIndex        SERIAL          PRIMARY KEY    区块索引
Timestamp         TIMESTAMP       NOT NULL       区块时间戳
DataType          VARCHAR(50)     NOT NULL       数据类型: MedicalRecord/Prescription
ReferenceId       INTEGER         NOT NULL       关联记录ID
DataHash          VARCHAR(64)     NOT NULL       数据哈希
PreviousHash      VARCHAR(64)     NOT NULL       前一区块哈希
BlockHash         VARCHAR(64)     NOT NULL       当前区块哈希
CreatedAt         TIMESTAMP       DEFAULT NOW()  创建时间

索引:
├── UNIQUE INDEX idx_block_hash ON Blockchain(BlockHash)
└── INDEX idx_reference ON Blockchain(DataType, ReferenceId)
```

### **DynamoDB NoSQL数据库**

#### **表1: HealthMetrics (健康监测数据)**

```
分区键 (Partition Key): PatientId (String)
排序键 (Sort Key): Timestamp (Number, Unix时间戳)

属性 (Attributes):
├── PatientId: String (分区键)
├── Timestamp: Number (排序键)
├── HeartRate: Number (心率 bpm)
├── BloodPressureSystolic: Number (收缩压 mmHg)
├── BloodPressureDiastolic: Number (舒张压 mmHg)
├── BloodSugar: Number (血糖 mmol/L)
├── OxygenSaturation: Number (血氧饱和度 %)
├── Temperature: Number (体温 °C)
├── Weight: Number (体重 kg)
├── DeviceId: String (设备ID)
├── DataSource: String (数据来源: Manual/IoTDevice/Wearable)
└── Notes: String (备注)

GSI (全局二级索引):
└── DeviceId-Timestamp-index
    ├── 分区键: DeviceId
    └── 排序键: Timestamp
    └── 用途: 按设备查询数据

TTL (生存时间):
└── 设置Timestamp字段自动过期 (保留90天数据)
```

#### **表2: UserSessions (用户会话)**

```
分区键: SessionId (String, UUID)

属性:
├── SessionId: String (主键)
├── UserId: Number
├── Token: String (JWT Token)
├── IPAddress: String
├── UserAgent: String (浏览器信息)
├── LoginTime: Number (Unix时间戳)
├── ExpiryTime: Number (过期时间)
└── IsActive: Boolean

TTL:
└── ExpiryTime字段 (24小时后自动删除)
```

#### **表3: AIAnalysisCache (AI分析缓存)**

```
分区键: SymptomsHash (String, MD5哈希)

属性:
├── SymptomsHash: String (主键, 症状描述的MD5)
├── OriginalSymptoms: String (原始症状文本)
├── AnalysisResult: String (AI分析结果)
├── RecommendedSpecialty: String (推荐科室)
├── Timestamp: Number (缓存时间)
└── HitCount: Number (命中次数)

TTL:
└── Timestamp字段 (7天后自动删除)
```

---

## 👥 系统用户角色与功能

### **角色1: 患者 (Patient)**

#### **核心功能**:

1. **账户管理**
   
   - 注册/登录/登出
   - 个人资料编辑
   - 修改密码
   - 上传头像

2. **智能问诊**
   
   - AI症状分析助手
     - 输入症状描述
     - AI推荐科室和医生
     - 预估就诊紧急程度
   - 药物识别
     - 上传药物图片
     - AI识别药名和用途

3. **预约管理**
   
   - 查看医生列表
     - 按科室筛选
     - 查看医生资料和评分
   - 在线预约挂号
     - 选择日期和时间段
     - 填写症状描述
     - AI自动分析并推荐
   - 查看我的预约
     - 待确认/已确认/已完成
   - 取消预约

4. **健康管理**
   
   - 健康数据记录
     - 手动输入心率、血压、血糖
     - 查看历史趋势图表
   - 电子病历查看
     - 查看历史就诊记录
     - 下载病历PDF
     - 区块链验证病历真实性
   - 处方管理
     - 查看电子处方
     - 用药提醒

5. **文档管理**
   
   - 上传检查报告
   - AI解读报告异常指标
   - 查看历史文档

### **角色2: 医生 (Doctor)**

#### **核心功能**:

1. **预约管理**
   
   - 查看待处理预约列表
   - 确认/拒绝预约
   - 查看患者AI分析结果
   - 调整预约时间

2. **诊疗管理**
   
   - 查看患者历史病历
   - 填写诊断结果
   - 开具电子处方
     - 处方自动上传区块链
   - 记录生命体征
   - 上传检查报告

3. **患者管理**
   
   - 查看我的患者列表
   - 查看患者健康监测数据
   - 查看患者历史就诊记录

4. **AI辅助**
   
   - AI相似病例推荐
   - AI用药建议
   - 药物相互作用检查

5. **个人管理**
   
   - 更新个人资料
   - 设置工作时间
   - 查看收入统计

### **角色3: 管理员 (Admin)**

#### **核心功能**:

1. **用户管理**
   
   - 查看所有用户列表
   - 添加/编辑/删除用户
   - 重置密码
   - 账户状态管理 (启用/禁用)

2. **医生管理**
   
   - 审核医生注册申请
   - 管理医生资料
   - 设置咨询费用

3. **系统监控**
   
   - CloudWatch仪表盘
     - 服务器性能监控
     - 数据库连接状态
     - API响应时间
     - 错误率统计
   - 查看系统日志
   - 告警管理

4. **数据分析**
   
   - 预约统计报表
     - 按日/周/月统计
     - 按科室分类
   - 用户增长趋势
   - 医生工作量统计
   - AI使用频率分析

5. **区块链管理**
   
   - 查看区块链完整性
   - 验证区块链未被篡改
   - 查看所有上链记录

---## 📅 8周详细开发时间表

---

## **Week 1 (Week 6): 项目规划与环境搭建**

### **Day 1-2: 需求分析与文档准备**

#### **任务清单**:

- [ ] 阅读并理解课业要求文档
- [ ] 确定项目范围和功能清单
- [ ] 绘制系统架构图 (使用draw.io或Lucidchart)
- [ ] 设计数据库ER图
- [ ] 制定详细开发计划和时间表
- [ ] 准备项目文档模板

#### **输出文档**:

1. **项目提案** (2-3页)
   - 行业问题分析
   - 解决方案概述
   - 技术栈选择理由
2. **系统架构图** (1页)
   - 完整的云服务架构图
   - 数据流向图
3. **数据库ER图** (1-2页)
   - 所有表关系
   - 字段详细说明

#### **学习资源**:

- AWS官方文档: https://docs.aws.amazon.com/
- ASP.NET Core教程: https://docs.microsoft.com/aspnet/core/
- React官方教程: https://react.dev/learn

---

### **Day 3-4: AWS账户与开发环境设置**

#### **AWS账户设置**:

```
步骤1: 注册AWS账户
├── 访问 aws.amazon.com
├── 准备信用卡 (仅验证用途，不扣费)
├── 选择Basic Support Plan (免费)
└── ⚠️ 立即设置预算警报 ($1 USD)

步骤2: 启用必要服务
├── EC2 (计算服务)
├── RDS (数据库服务)
├── S3 (存储服务)
├── DynamoDB (NoSQL数据库)
├── Lambda (Serverless)
├── CloudWatch (监控)
└── CloudFront (CDN)

步骤3: 创建IAM用户
├── 创建管理员用户 (而非使用root账户)
├── 下载访问密钥 (Access Key + Secret Key)
└── 安装AWS CLI工具
```

#### **本地开发环境安装**:

```
Windows系统:
1. Visual Studio 2022 Community
   └── 下载: https://visualstudio.microsoft.com/downloads/
   └── 安装组件:
       ✅ ASP.NET and web development
       ✅ .NET 8.0 SDK
       ✅ Azure development (包含AWS工具)

2. Node.js 20+ LTS
   └── 下载: https://nodejs.org/
   └── 验证: node --version 和 npm --version

3. Git
   └── 下载: https://git-scm.com/downloads
   └── 配置: git config --global user.name "你的名字"

4. VS Code (可选，用于前端开发)
   └── 下载: https://code.visualstudio.com/
   └── 扩展:
       ├── ES7+ React/Redux/React-Native snippets
       ├── Prettier - Code formatter
       └── ESLint

5. PostgreSQL客户端工具
   └── pgAdmin 4: https://www.pgadmin.org/download/
   └── 或 DBeaver: https://dbeaver.io/download/

6. Postman (API测试)
   └── 下载: https://www.postman.com/downloads/

macOS/Linux系统:
1. .NET 8.0 SDK
   └── macOS: brew install dotnet
   └── Linux: 参考微软官方文档

2. 其他工具同Windows
```

---

### **Day 5-7: 创建AWS资源**

#### **任务1: 创建RDS数据库实例**

```
AWS Console → RDS → Create database

配置参数:
┌─────────────────────────────────────────┐
│ Engine: PostgreSQL 15.x                 │
│ Templates: Free tier ✅                 │
│ DB instance identifier: healthcare-db   │
│ Master username: postgres               │
│ Master password: [设置强密码，记录下来]   │
│ DB instance class: db.t3.micro          │
│ Storage type: General Purpose SSD       │
│ Allocated storage: 20 GB                │
│ Public access: Yes (开发阶段)            │
│ VPC security group: 创建新的             │
│ Initial database name: healthcaredb     │
└─────────────────────────────────────────┘

安全组设置:
├── Type: PostgreSQL
├── Protocol: TCP
├── Port: 5432
└── Source: 0.0.0.0/0 (开发阶段允许所有IP)
    ⚠️ 生产环境需限制为特定IP

等待时间: 约5-10分钟创建完成

验证连接:
└── 使用pgAdmin或DBeaver连接
    Host: [RDS端点].rds.amazonaws.com
    Port: 5432
    Database: healthcaredb
    Username: postgres
    Password: [你设置的密码]
```

#### **任务2: 创建S3 Bucket**

```
AWS Console → S3 → Create bucket

Bucket 1: 前端托管
┌─────────────────────────────────────────┐
│ Bucket name: healthcare-frontend-[学号]  │
│ Region: ap-southeast-1 (新加坡)          │
│ Block Public Access: 取消勾选 ✅          │
│ Bucket Versioning: 启用                  │
│ Server-side encryption: AES-256         │
└─────────────────────────────────────────┘

配置静态网站托管:
├── Properties → Static website hosting
├── Enable ✅
├── Index document: index.html
└── Error document: index.html

Bucket 2: 文件存储
┌─────────────────────────────────────────┐
│ Bucket name: healthcare-files-[学号]     │
│ Region: ap-southeast-1                  │
│ Block Public Access: 部分勾选            │
│ 生命周期规则: 30天后转到Glacier          │
└─────────────────────────────────────────┘
```

#### **任务3: 创建DynamoDB表**

```
AWS Console → DynamoDB → Create table

表1: HealthMetrics
┌─────────────────────────────────────────┐
│ Table name: HealthMetrics               │
│ Partition key: PatientId (String)       │
│ Sort key: Timestamp (Number)            │
│ Table settings: Default settings        │
│ Read/write capacity: On-demand          │
└─────────────────────────────────────────┘

表2: UserSessions
┌─────────────────────────────────────────┐
│ Table name: UserSessions                │
│ Partition key: SessionId (String)       │
│ TTL attribute: ExpiryTime               │
└─────────────────────────────────────────┘

表3: AIAnalysisCache
┌─────────────────────────────────────────┐
│ Table name: AIAnalysisCache             │
│ Partition key: SymptomsHash (String)    │
│ TTL attribute: ExpiryTime               │
└─────────────────────────────────────────┘
```

#### **任务4: 创建EC2实例**

```
AWS Console → EC2 → Launch Instance

配置:
┌─────────────────────────────────────────┐
│ Name: healthcare-api-server             │
│ AMI: Ubuntu Server 22.04 LTS            │
│ Instance type: t2.micro (免费)          │
│ Key pair: 创建新密钥对并下载 .pem文件     │
│ Security group:                         │
│   ├── SSH (22) - My IP                  │
│   ├── HTTP (80) - Anywhere              │
│   ├── HTTPS (443) - Anywhere            │
│   └── Custom TCP (5000) - Anywhere      │
│ Storage: 8 GB gp3                       │
└─────────────────────────────────────────┘

创建完成后:
└── 记录Public IPv4 address
└── 记录Public IPv4 DNS
```

---

### **Week 1交付成果**:

- ✅ 完整的项目规划文档
- ✅ AWS账户已设置并配置
- ✅ 所有AWS资源已创建
- ✅ 本地开发环境已安装
- ✅ 可以连接到RDS数据库
- ✅ 可以SSH连接到EC2实例

---

## **Week 2 (Week 7): 后端基础开发**

### **Day 1-2: 创建ASP.NET Core项目**

#### **创建项目结构**:

```bash
# 创建解决方案
dotnet new sln -n HealthcareProject

# 创建Web API项目
dotnet new webapi -n HealthcareAPI
dotnet sln add HealthcareAPI/HealthcareAPI.csproj

# 进入项目目录
cd HealthcareAPI

# 安装NuGet包
dotnet add package Npgsql.EntityFrameworkCore.PostgreSQL --version 8.0.0
dotnet add package Microsoft.EntityFrameworkCore.Tools --version 8.0.0
dotnet add package Microsoft.EntityFrameworkCore.Design --version 8.0.0
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer --version 8.0.0
dotnet add package BCrypt.Net-Next --version 4.0.3
dotnet add package AWSSDK.S3 --version 3.7.0
dotnet add package AWSSDK.DynamoDBv2 --version 3.7.0
dotnet add package Swashbuckle.AspNetCore --version 6.5.0
dotnet add package Serilog.AspNetCore --version 8.0.0
```

#### **项目文件夹结构**:

```
HealthcareAPI/
├── Controllers/          # API控制器
├── Models/              # 数据模型
│   ├── User.cs
│   ├── Doctor.cs
│   ├── Appointment.cs
│   ├── MedicalRecord.cs
│   ├── Prescription.cs
│   └── DTOs/           # 数据传输对象
├── Data/               # 数据库上下文
│   └── ApplicationDbContext.cs
├── Services/           # 业务逻辑服务
│   ├── AIService.cs
│   ├── BlockchainService.cs
│   ├── S3Service.cs
│   └── DynamoDBService.cs
├── Middleware/         # 自定义中间件
├── appsettings.json    # 配置文件
└── Program.cs          # 入口点
```

---

### **Day 3-4: 实现数据模型和数据库上下文**

#### **任务清单**:

- [ ] 创建所有数据模型类 (User, Doctor, Appointment等)
- [ ] 创建ApplicationDbContext
- [ ] 配置实体关系 (一对一、一对多)
- [ ] 配置appsettings.json连接字符串
- [ ] 运行数据库迁移
- [ ] 验证表已创建

#### **数据库迁移命令**:

```bash
# 创建初始迁移
dotnet ef migrations add InitialCreate

# 查看生成的SQL
dotnet ef migrations script

# 应用到数据库
dotnet ef database update

# 验证
# 使用pgAdmin连接数据库，检查表是否创建成功
```

---

### **Day 5-7: 实现认证系统**

#### **任务清单**:

- [ ] 创建AuthController (注册、登录、登出)
- [ ] 实现JWT Token生成
- [ ] 实现密码加密 (BCrypt)
- [ ] 配置JWT认证中间件
- [ ] 创建DTO类 (LoginDTO, RegisterDTO)
- [ ] 测试注册和登录功能

#### **API端点**:

```
POST /api/auth/register
├── 输入: { email, password, fullName, phoneNumber, userRole }
└── 输出: { message, userId }

POST /api/auth/login
├── 输入: { email, password }
└── 输出: { token, userId, email, fullName, role }

POST /api/auth/logout
└── 输出: { message }

GET /api/auth/profile
├── 需要认证
└── 输出: { userId, email, fullName, role, ... }
```

#### **测试方法**:

```
使用Postman测试:
1. 注册新用户
2. 登录获取Token
3. 复制Token到Authorization Header
4. 测试受保护的端点
```

---

### **Week 2交付成果**:

- ✅ ASP.NET Core项目已创建
- ✅ 数据库表已创建
- ✅ 用户认证系统已实现
- ✅ JWT Token系统正常工作
- ✅ API文档 (Swagger) 可访问

---

## **Week 3 (Week 8): 后端核心功能开发**

### **Day 1-2: 实现医生管理功能**

#### **API端点**:

```
GET /api/doctors
└── 获取所有医生列表 (可按专科筛选)

GET /api/doctors/{id}
└── 获取单个医生详情

POST /api/doctors (Admin only)
└── 创建医生资料

PUT /api/doctors/{id} (Doctor/Admin)
└── 更新医生资料

DELETE /api/doctors/{id} (Admin only)
└── 删除医生
```

---

### **Day 3-4: 实现预约管理功能**

#### **API端点**:

```
GET /api/appointments (需要认证)
└── 获取当前用户的预约列表
    └── 患者: 我的所有预约
    └── 医生: 我的患者预约

POST /api/appointments
└── 创建新预约

PUT /api/appointments/{id}/confirm (Doctor only)
└── 医生确认预约

PUT /api/appointments/{id}/cancel
└── 取消预约

GET /api/appointments/{id}
└── 获取预约详情
```

---

### **Day 5-7: 实现病历和处方功能**

#### **API端点**:

```
病历相关:
GET /api/medical-records (需要认证)
POST /api/medical-records (Doctor only)
GET /api/medical-records/{id}
PUT /api/medical-records/{id} (Doctor only)

处方相关:
GET /api/prescriptions
POST /api/prescriptions (Doctor only)
GET /api/prescriptions/{id}
```

---

### **Week 3交付成果**:

- ✅ 医生管理API完成
- ✅ 预约管理API完成
- ✅ 病历管理API完成
- ✅ 处方管理API完成
- ✅ 所有API已通过Postman测试

---

## **Week 4 (Week 9): 前端基础开发**

### **Day 1-2: 创建React项目**

#### **创建项目**:

```bash
# 使用Vite创建 (比create-react-app更快)
npm create vite@latest frontend -- --template react-ts
cd frontend

# 安装依赖
npm install

# 安装必要的库
npm install axios react-router-dom
npm install @mui/material @emotion/react @emotion/styled
npm install @mui/icons-material
npm install react-toastify
npm install recharts
npm install date-fns
npm install formik yup
npm install @tanstack/react-query
```

#### **项目结构**:

```
frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── components/      # 可复用组件
│   │   ├── Navbar.tsx
│   │   ├── Footer.tsx
│   │   ├── DoctorCard.tsx
│   │   ├── AppointmentCard.tsx
│   │   └── ProtectedRoute.tsx
│   ├── pages/          # 页面组件
│   │   ├── Login.tsx
│   │   ├── Register.tsx
│   │   ├── Dashboard.tsx
│   │   ├── DoctorList.tsx
│   │   ├── BookAppointment.tsx
│   │   ├── MyAppointments.tsx
│   │   └── AISymptomAnalyzer.tsx
│   ├── services/       # API服务层
│   │   ├── api.ts
│   │   ├── authService.ts
│   │   ├── doctorService.ts
│   │   └── appointmentService.ts
│   ├── contexts/       # React Context
│   │   └── AuthContext.tsx
│   ├── hooks/          # 自定义Hooks
│   │   └── useAuth.ts
│   ├── types/          # TypeScript类型定义
│   │   └── index.ts
│   ├── utils/          # 工具函数
│   │   └── helpers.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
├── .env.development     # 开发环境变量
├── .env.production      # 生产环境变量
├── package.json
└── tsconfig.json
```

#### **环境变量配置**:

```
.env.development:
VITE_API_URL=https://localhost:5001/api

.env.production:
VITE_API_URL=https://your-ec2-domain.com/api
```

---

### **Day 3-4: 实现认证相关页面**

#### **任务清单**:

- [ ] 创建Login页面
- [ ] 创建Register页面
- [ ] 创建AuthContext (全局认证状态)
- [ ] 实现ProtectedRoute组件
- [ ] 配置React Router

---

### **Day 5-7: 实现核心页面**

#### **任务清单**:

- [ ] Dashboard页面 (根据角色显示不同内容)
- [ ] DoctorList页面 (患者查看医生)
- [ ] BookAppointment页面 (预约表单)
- [ ] MyAppointments页面 (预约列表)
- [ ] Navbar组件 (导航栏)
- [ ] Footer组件

---

### **Week 4交付成果**:

- ✅ React项目已创建
- ✅ 认证页面完成 (登录/注册)
- ✅ 基础页面完成
- ✅ 前后端可以正常通信
- ✅ JWT认证流程正常工作

---

## **Week 5 (Week 10): AI功能整合**

### **Day 1-2: 设置Google Gemini API**

#### **注册Gemini API**:

```
步骤:
1. 访问 https://aistudio.google.com/
2. 使用Google账户登录
3. 点击 "Get API Key"
4. 创建新的API密钥
5. 复制密钥到appsettings.json

免费额度:
├── 60次请求/分钟
├── 1,500次请求/天
└── gemini-pro和gemini-pro-vision模型
```

#### **后端实现AIService**:

```
功能1: 症状分析
├── 输入: 症状描述文本
├── 处理: 调用Gemini API
└── 输出: 
    ├── 可能的病因
    ├── 推荐科室
    └── 注意事项

功能2: 药物识别
├── 输入: 药物图片 (Base64)
├── 处理: 调用Gemini Pro Vision
└── 输出: 药物名称和用途

功能3: 报告解读
├── 输入: 检验报告文本
├── 处理: AI分析异常指标
└── 输出: 健康建议
```

#### **API端点**:

```
POST /api/ai/analyze-symptoms
├── 输入: { symptoms: "头痛、发烧、咳嗽" }
└── 输出: { analysis, recommendedSpecialty, severity }

POST /api/ai/identify-medicine
├── 输入: { imageBase64: "..." }
└── 输出: { medicineName, usage, precautions }

POST /api/ai/interpret-report
├── 输入: { reportText: "..." }
└── 输出: { abnormalItems, recommendations }
```

---

### **Day 3-4: 前端AI功能页面**

#### **任务清单**:

- [ ] 创建AISymptomAnalyzer页面
- [ ] 实现症状输入表单
- [ ] 显示AI分析结果
- [ ] 创建MedicineIdentifier组件
- [ ] 实现图片上传功能
- [ ] 创建ReportInterpreter组件

---

### **Day 5-7: 实现Vector Database (pgvector)**

#### **在RDS中启用pgvector**:

```sql
-- 连接到RDS数据库
psql -h [RDS端点] -U postgres -d healthcaredb

-- 安装扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- 创建症状向量表
CREATE TABLE symptom_vectors (
    vector_id SERIAL PRIMARY KEY,
    symptom_text TEXT NOT NULL,
    embedding VECTOR(1536),
    recommended_specialty VARCHAR(100),
    diagnosis TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- 创建向量索引
CREATE INDEX ON symptom_vectors 
USING ivfflat (embedding vector_cosine_ops) 
WITH (lists = 100);
```

#### **实现向量搜索功能**:

```
流程:
1. 用户输入症状
2. 调用OpenAI Embedding API转为向量
3. 在pgvector中搜索相似症状
4. 返回历史相似病例
5. 结合Gemini分析给出建议
```

---

### **Week 5交付成果**:

- ✅ Gemini API已整合
- ✅ AI症状分析功能完成
- ✅ 药物识别功能完成
- ✅ pgvector向量搜索实现
- ✅ 前端AI页面完成

---

## **Week 6 (Week 11): 区块链与高级功能**

### **Day 1-3: 实现区块链系统**

#### **后端BlockchainService实现**:

```
区块结构:
├── BlockIndex (区块索引)
├── Timestamp (时间戳)
├── DataType (MedicalRecord/Prescription)
├── ReferenceId (关联记录ID)
├── DataHash (数据SHA-256哈希)
├── PreviousHash (前一区块哈希)
└── BlockHash (当前区块哈希)

核心功能:
├── AddBlock() - 添加新区块
├── ValidateChain() - 验证链完整性
├── GetBlockByReference() - 查询区块
└── GetFullChain() - 获取完整链
```

#### **整合到病历系统**:

```
流程:
1. 医生创建病历 → 保存到PostgreSQL
2. 自动调用BlockchainService.AddBlock()
3. 计算数据哈希并创建新区块
4. 链接到前一区块
5. 保存到Blockchain表
6. 返回区块哈希给前端显示
```

#### **API端点**:

```
GET /api/blockchain/verify
└── 验证整个链的完整性

GET /api/blockchain/record/{recordId}
└── 查询特定记录的区块信息

GET /api/blockchain/chain
└── 获取完整区块链 (Admin only)
```

---

### **Day 4-5: S3文件上传功能**

#### **后端S3Service实现**:

```
功能:
├── UploadFile() - 上传文件到S3
├── GetFileUrl() - 获取文件URL
├── DeleteFile() - 删除文件
└── ListFiles() - 列出用户文件
```

#### **API端点**:

```
POST /api/files/upload
├── 输入: multipart/form-data
└── 输出: { fileUrl, fileKey }

GET /api/files
└── 获取当前用户的所有文件

DELETE /api/files/{fileKey}
└── 删除文件
```

---

### **Day 6-7: DynamoDB健康数据功能**

#### **后端DynamoDBService实现**:

```
功能:
├── SaveHealthMetric() - 保存健康数据
├── GetPatientMetrics() - 获取患者数据
├── GetMetricsByDateRange() - 按日期范围查询
└── GetLatestMetric() - 获取最新数据
```

#### **API端点**:

```
POST /api/health-metrics
└── 保存健康数据

GET /api/health-metrics
└── 获取当前用户的健康数据

GET /api/health-metrics/chart?days=30
└── 获取图表数据 (最近30天)
```

#### **前端实现**:

- [ ] 创建HealthMetricsPage
- [ ] 实现数据输入表单
- [ ] 使用Recharts显示趋势图

---

### **Week 6交付成果**:

- ✅ 区块链系统完成
- ✅ S3文件上传功能完成
- ✅ DynamoDB健康数据功能完成
- ✅ 所有高级功能已整合

---

## **Week 7 (Week 12-13): AWS部署与监控**

### **Day 1-2: 部署后端到EC2**

#### **连接到EC2**:

```bash
# Windows (使用Git Bash或PowerShell)
ssh -i "your-key.pem" ubuntu@[EC2-Public-IP]

# 首次连接需要修改权限
chmod 400 your-key.pem
```

#### **在EC2上安装.NET**:

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装.NET 8.0 SDK
wget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
sudo apt update
sudo apt install -y dotnet-sdk-8.0

# 验证
dotnet --version
```

#### **部署应用**:

```bash
# 在本地构建发布版本
dotnet publish -c Release -o ./publish

# 压缩文件
tar -czf publish.tar.gz publish/

# 上传到EC2 (Windows使用WinSCP或scp)
scp -i "your-key.pem" publish.tar.gz ubuntu@[EC2-IP]:/home/ubuntu/

# 在EC2上解压
tar -xzf publish.tar.gz
cd publish

# 运行应用
dotnet HealthcareAPI.dll

# 设置环境变量
export ASPNETCORE_URLS="http://0.0.0.0:5000"
export ASPNETCORE_ENVIRONMENT="Production"
```

#### **配置Systemd服务 (后台运行)**:

```bash
# 创建服务文件
sudo nano /etc/systemd/system/healthcare-api.service

# 内容:
[Unit]
Description=Healthcare API

[Service]
WorkingDirectory=/home/ubuntu/publish
ExecStart=/usr/bin/dotnet /home/ubuntu/publish/HealthcareAPI.dll
Restart=always
RestartSec=10
User=ubuntu
Environment=ASPNETCORE_ENVIRONMENT=Production
Environment=ASPNETCORE_URLS=http://0.0.0.0:5000

[Install]
WantedBy=multi-user.target

# 启动服务
sudo systemctl enable healthcare-api
sudo systemctl start healthcare-api
sudo systemctl status healthcare-api
```

---

### **Day 3-4: 部署前端到S3 + CloudFront**

#### **构建React应用**:

```bash
# 在frontend目录
npm run build

# 输出在 dist/ 目录
```

#### **上传到S3**:

```bash
# 安装AWS CLI
# Windows: https://aws.amazon.com/cli/
# macOS: brew install awscli

# 配置AWS CLI
aws configure
# 输入Access Key ID
# 输入Secret Access Key
# Region: ap-southeast-1

# 上传到S3
aws s3 sync dist/ s3://healthcare-frontend-[学号]/ --delete

# 设置Bucket策略 (公开访问)
aws s3api put-bucket-policy --bucket healthcare-frontend-[学号] --policy file://bucket-policy.json
```

#### **bucket-policy.json**:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::healthcare-frontend-[学号]/*"
    }
  ]
}
```

#### **配置CloudFront**:

```
AWS Console → CloudFront → Create Distribution

配置:
├── Origin domain: healthcare-frontend-[学号].s3.ap-southeast-1.amazonaws.com
├── Origin path: 留空
├── Viewer protocol policy: Redirect HTTP to HTTPS
├── Allowed HTTP methods: GET, HEAD, OPTIONS
├── Cache policy: CachingOptimized
└── Price class: Use only North America
```

## **Week 7 (Week 12-13): AWS部署与监控** (续)

### **Day 3-4: 部署前端到S3 + CloudFront** (续)

#### **CloudFront配置 (续)**:

```
Error Pages配置:
├── 404 Not Found → /index.html (200 Response)
└── 403 Forbidden → /index.html (200 Response)
    └── 原因: React Router需要所有路由返回index.html

部署完成后:
└── CloudFront会生成一个域名: d1234abcd.cloudfront.net
└── 记录这个域名，更新前端环境变量
```

#### **更新前端API地址**:

```bash
# 修改 .env.production
VITE_API_URL=http://[EC2-Public-IP]:5000/api

# 重新构建
npm run build

# 重新上传到S3
aws s3 sync dist/ s3://healthcare-frontend-[学号]/ --delete

# 清除CloudFront缓存
aws cloudfront create-invalidation --distribution-id [你的分发ID] --paths "/*"
```

---

### **Day 5: 配置CloudWatch监控**

#### **在EC2上安装CloudWatch Agent**:

```bash
# 下载CloudWatch Agent
wget https://s3.amazonaws.com/amazoncloudwatch-agent/ubuntu/amd64/latest/amazon-cloudwatch-agent.deb

# 安装
sudo dpkg -i amazon-cloudwatch-agent.deb

# 创建配置文件
sudo nano /opt/aws/amazon-cloudwatch-agent/etc/config.json
```

#### **CloudWatch Agent配置文件**:

```json
{
  "agent": {
    "metrics_collection_interval": 60,
    "run_as_user": "cwagent"
  },
  "logs": {
    "logs_collected": {
      "files": {
        "collect_list": [
          {
            "file_path": "/home/ubuntu/publish/logs/*.log",
            "log_group_name": "/aws/ec2/healthcare-api",
            "log_stream_name": "{instance_id}",
            "timezone": "UTC"
          }
        ]
      }
    }
  },
  "metrics": {
    "namespace": "Healthcare/API",
    "metrics_collected": {
      "cpu": {
        "measurement": [
          {
            "name": "cpu_usage_idle",
            "rename": "CPU_IDLE",
            "unit": "Percent"
          }
        ],
        "metrics_collection_interval": 60,
        "totalcpu": false
      },
      "disk": {
        "measurement": [
          {
            "name": "used_percent",
            "rename": "DISK_USED",
            "unit": "Percent"
          }
        ],
        "metrics_collection_interval": 60,
        "resources": ["*"]
      },
      "mem": {
        "measurement": [
          {
            "name": "mem_used_percent",
            "rename": "MEM_USED",
            "unit": "Percent"
          }
        ],
        "metrics_collection_interval": 60
      }
    }
  }
}
```

#### **启动CloudWatch Agent**:

```bash
# 启动Agent
sudo /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl \
    -a fetch-config \
    -m ec2 \
    -s \
    -c file:/opt/aws/amazon-cloudwatch-agent/etc/config.json

# 检查状态
sudo /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl \
    -a query \
    -m ec2 \
    -s
```

---

#### **在ASP.NET中记录自定义指标**:

```
安装包:
dotnet add package AWSSDK.CloudWatch

实现MetricsService:
├── RecordAPIRequest() - 记录API请求数
├── RecordAPIResponseTime() - 记录响应时间
├── RecordAppointmentCreated() - 记录预约创建数
├── RecordAIAPICall() - 记录AI API调用次数
└── RecordError() - 记录错误数
```

---

### **Day 6: 创建CloudWatch Dashboard**

#### **在AWS Console创建Dashboard**:

```
AWS Console → CloudWatch → Dashboards → Create dashboard

Dashboard名称: Healthcare-Monitoring

添加小部件:
┌────────────────────────────────────────┐
│ Widget 1: EC2 CPU使用率                 │
│ ├── Type: Line graph                   │
│ ├── Metric: AWS/EC2 → CPUUtilization  │
│ └── Statistic: Average, Period: 5min  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 2: 内存使用率                    │
│ ├── Type: Line graph                   │
│ ├── Metric: Healthcare/API → MEM_USED │
│ └── Statistic: Average, Period: 5min  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 3: RDS数据库连接数               │
│ ├── Type: Number                       │
│ ├── Metric: AWS/RDS → DatabaseConnections │
│ └── Statistic: Sum                     │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 4: API请求数 (自定义指标)         │
│ ├── Type: Line graph                   │
│ ├── Metric: Healthcare/API → APIRequests │
│ └── Statistic: Sum, Period: 5min      │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 5: 预约创建趋势                  │
│ ├── Type: Bar chart                    │
│ ├── Metric: Healthcare/API → AppointmentsCreated │
│ └── Statistic: Sum, Period: 1hour     │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 6: AI API调用次数                │
│ ├── Type: Number                       │
│ ├── Metric: Healthcare/API → AIAPICalls │
│ └── Statistic: Sum, Period: 1day      │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 7: API错误率                     │
│ ├── Type: Gauge                        │
│ ├── Metric: Healthcare/API → ErrorRate │
│ └── Statistic: Average                 │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Widget 8: S3存储使用量                  │
│ ├── Type: Number                       │
│ ├── Metric: AWS/S3 → BucketSizeBytes  │
│ └── Statistic: Average                 │
└────────────────────────────────────────┘
```

---

### **Day 7: 配置Lambda函数**

#### **Lambda函数1: 预约提醒**

**创建Lambda函数**:

```
AWS Console → Lambda → Create function

配置:
├── Function name: healthcare-appointment-reminder
├── Runtime: .NET 8 (C#/PowerShell)
├── Architecture: x86_64
└── Execution role: Create new role with basic Lambda permissions
```

**添加RDS访问权限**:

```
IAM Console → Roles → lambda-execution-role

添加策略:
├── AWSLambdaVPCAccessExecutionRole
└── 自定义RDS访问策略
```

**Lambda函数代码结构**:

```csharp
public class Function
{
    public async Task FunctionHandler(ILambdaContext context)
    {
        // 1. 连接到RDS数据库
        // 2. 查询明天的预约
        // 3. 发送邮件/SMS提醒
        // 4. 记录到CloudWatch Logs
    }
}
```

**配置EventBridge触发器**:

```
EventBridge → Rules → Create rule

配置:
├── Name: daily-appointment-reminder
├── Event source: Schedule
├── Cron expression: 0 12 * * ? *
│   └── 每天UTC 12:00 (马来西亚时间 20:00)
└── Target: Lambda function
```

---

#### **Lambda函数2: 数据备份**

**功能**: 每周自动备份PostgreSQL数据到S3

**Cron表达式**: `0 18 ? * SUN *` └── 每周日UTC 18:00 (马来西亚时间周一02:00)

---

### **Day 7: 配置CloudWatch Alarms**

#### **告警1: 高CPU使用率**

```
AWS Console → CloudWatch → Alarms → Create alarm

配置:
├── Metric: EC2 CPUUtilization
├── Threshold: >= 80%
├── Datapoints: 2 out of 2
├── Period: 5 minutes
└── Action: 发送SNS通知到邮箱
```

#### **告警2: 高错误率**

```
配置:
├── Metric: Healthcare/API ErrorRate
├── Threshold: >= 5%
├── Period: 5 minutes
└── Action: SNS通知
```

#### **告警3: 数据库连接数过高**

```
配置:
├── Metric: RDS DatabaseConnections
├── Threshold: >= 80
├── Period: 5 minutes
└── Action: SNS通知
```

---

### **Week 7交付成果**:

- ✅ 后端已部署到EC2并正常运行
- ✅ 前端已部署到S3 + CloudFront
- ✅ CloudWatch监控已配置
- ✅ 自定义Dashboard已创建
- ✅ Lambda函数已部署
- ✅ 告警已设置
- ✅ 应用可通过CloudFront域名访问

---

## **Week 8 (Week 14): 测试、优化与文档**

### **Day 1: 全面功能测试**

#### **测试清单**:

**患者端测试**:

```
✅ 用户注册
   └── 测试邮箱格式验证
   └── 测试密码强度检查
   └── 测试重复邮箱提示

✅ 用户登录
   └── 测试正确凭证
   └── 测试错误凭证
   └── 测试JWT Token生成

✅ 查看医生列表
   └── 测试按科室筛选
   └── 测试分页功能

✅ AI症状分析
   └── 输入不同症状测试
   └── 验证推荐科室准确性
   └── 测试响应时间

✅ 预约挂号
   └── 选择医生和时间
   └── 填写症状
   └── 验证预约创建成功

✅ 查看我的预约
   └── 验证预约列表显示
   └── 测试取消预约

✅ 健康数据记录
   └── 输入健康数据
   └── 验证图表显示
   └── 测试历史数据查询

✅ 文件上传
   └── 上传检查报告
   └── 验证S3存储成功
   └── 测试文件下载
```

**医生端测试**:

```
✅ 查看预约列表
   └── 验证待处理预约显示
   └── 测试日期筛选

✅ 确认预约
   └── 测试确认功能
   └── 验证状态更新

✅ 填写病历
   └── 输入诊断结果
   └── 验证区块链记录
   └── 测试病历保存

✅ 开具处方
   └── 添加药物
   └── 验证处方上链
   └── 测试处方查看
```

**管理员端测试**:

```
✅ 用户管理
   └── 查看用户列表
   └── 编辑用户信息
   └── 禁用/启用账户

✅ 系统监控
   └── 查看CloudWatch Dashboard
   └── 验证指标数据正确
   └── 测试告警功能

✅ 区块链验证
   └── 验证链完整性
   └── 查看所有区块
   └── 测试篡改检测
```

---

### **Day 2: 性能优化**

#### **优化清单**:

**前端优化**:

```
✅ 代码分割 (Code Splitting)
   └── 使用React.lazy()懒加载页面
   └── 减少首屏加载时间

✅ 图片优化
   └── 压缩图片尺寸
   └── 使用WebP格式
   └── 启用CloudFront图片缓存

✅ API调用优化
   └── 使用React Query缓存数据
   └── 实现防抖(debounce)和节流(throttle)
   └── 减少不必要的API请求

✅ Bundle大小优化
   └── 移除未使用的依赖
   └── 使用生产环境构建
   └── 启用Tree Shaking
```

**后端优化**:

```
✅ 数据库查询优化
   └── 添加必要的索引
   └── 使用Include进行关联查询
   └── 避免N+1查询问题

✅ 实现缓存
   └── 使用IMemoryCache缓存热点数据
   └── 缓存医生列表(10分钟)
   └── 缓存AI分析结果(DynamoDB)

✅ API响应优化
   └── 使用DTO减少数据传输
   └── 启用响应压缩(Gzip)
   └── 实现分页和过滤

✅ 异步处理
   └── 文件上传使用异步处理
   └── 区块链操作异步化
   └── 邮件发送异步队列
```

**数据库优化**:

```
✅ 添加索引
CREATE INDEX idx_appointments_date ON Appointments(AppointmentDate);
CREATE INDEX idx_appointments_status ON Appointments(Status);
CREATE INDEX idx_users_email ON Users(Email);
CREATE INDEX idx_doctors_specialization ON Doctors(Specialization);

✅ 查询优化
   └── 使用EXPLAIN分析慢查询
   └── 避免SELECT *
   └── 使用适当的JOIN类型

✅ 连接池配置
   └── 设置最大连接数
   └── 配置连接超时时间
```

---

### **Day 3: 安全加固**

#### **安全检查清单**:

**认证与授权**:

```
✅ JWT Token安全
   └── 使用强密钥 (至少32字符)
   └── 设置合理的过期时间 (1小时)
   └── 实现Token刷新机制

✅ 密码安全
   └── BCrypt加密 (Work Factor = 12)
   └── 密码强度要求 (最少8位)
   └── 防止暴力破解 (登录限制)

✅ API授权
   └── 所有敏感端点要求认证
   └── 基于角色的访问控制(RBAC)
   └── 验证用户权限
```

**数据安全**:

```
✅ SQL注入防护
   └── 使用Entity Framework参数化查询
   └── 禁止动态SQL

✅ XSS防护
   └── Razor自动HTML编码
   └── Content Security Policy headers
   └── 验证用户输入

✅ CSRF防护
   └── ASP.NET自动CSRF Token
   └── SameSite Cookie属性

✅ 数据加密
   └── HTTPS强制 (SSL/TLS)
   └── 敏感数据加密存储
   └── S3文件加密 (AES-256)
```

**AWS安全**:

```
✅ EC2安全组
   └── 最小权限原则
   └── 限制SSH访问IP
   └── 定期审查规则

✅ RDS安全
   └── 不允许公网直接访问 (生产环境)
   └── 使用SSL连接
   └── 定期备份

✅ S3安全
   └── Bucket策略最小权限
   └── 启用版本控制
   └── 启用访问日志

✅ IAM权限
   └── 使用IAM角色而非Access Key
   └── 最小权限原则
   └── 定期轮换密钥
```

---

### **Day 4-6: 编写完整项目文档**

#### **文档结构 (30-40页)**:

**1. 封面 (1页)**

```
包含:
├── 项目标题
├── 学生姓名和学号
├── 课程代码: CT071-3-3-DDAC
├── 提交日期
└── 学校Logo
```

---

**2. 目录 (1-2页)**

```
自动生成目录
包含所有章节和页码
```

---

**3. Executive Summary (1-2页)**

```
包含:
├── 项目背景
├── 主要目标
├── 核心功能概述
├── 使用的技术栈
├── 主要成果
└── 项目价值
```

---

**4. Introduction (2-3页)**

```
4.1 项目背景
   └── 医疗行业现状
   └── 数字化转型需求

4.2 问题陈述
   └── Healthcare Industry面临的5大挑战
   └── 详细分析每个问题

4.3 项目目标
   └── 解决方案目标
   └── 预期成果

4.4 项目范围
   └── 包含的功能
   └── 不包含的功能
```

---

**5. Literature Review (3-4页)**

```
5.1 云计算概念
   └── IaaS, PaaS, SaaS
   └── AWS云服务优势

5.2 AI在医疗领域的应用
   └── AI症状分析
   └── 机器学习诊断辅助

5.3 区块链在医疗的应用
   └── 电子病历防篡改
   └── 数据共享与隐私保护

5.4 相关系统研究
   └── 现有医疗预约系统
   └── 我们的创新点
```

---

**6. System Architecture (5-6页)**

```
6.1 整体架构
   └── 系统架构图 (高质量图片)
   └── 各组件说明

6.2 技术栈详细说明
   └── Frontend: React + TypeScript
   └── Backend: ASP.NET Core Web API
   └── Database: PostgreSQL + DynamoDB
   └── Cloud Services: 详细列出所有AWS服务

6.3 AWS服务整合
   └── EC2部署架构
   └── RDS数据库配置
   └── S3存储策略
   └── CloudFront CDN加速
   └── Lambda Serverless架构
   └── DynamoDB NoSQL数据存储
   └── CloudWatch监控体系

6.4 安全架构
   └── JWT认证流程
   └── 数据加密方案
   └── AWS安全配置
```

---

**7. Database Design (4-5页)**

```
7.1 关系型数据库设计
   └── ER图 (完整、清晰)
   └── 表结构详细说明
   └── 关系说明 (一对一、一对多)
   └── 索引设计

7.2 NoSQL数据库设计
   └── DynamoDB表设计
   └── 分区键和排序键选择
   └── GSI设计

7.3 数据流图
   └── 数据如何在各组件间流动

7.4 数据库优化
   └── 索引策略
   └── 查询优化
```

---

**8. System Implementation (8-10页)**

```
8.1 用户管理模块
   └── 注册流程
   └── 登录认证
   └── JWT Token机制
   └── 代码示例 (关键部分)

8.2 预约管理模块
   └── 医生列表展示
   └── 预约创建流程
   └── 预约状态管理
   └── 流程图

8.3 AI功能模块
   └── Google Gemini API整合
   └── 症状分析实现
   └── 药物识别实现
   └── Vector Database (pgvector)
   └── 相似症状搜索
   └── 实际运行截图

8.4 区块链模块
   └── 区块结构设计
   └── 哈希计算方法
   └── 链接机制
   └── 完整性验证
   └── 代码示例
   └── 区块链截图

8.5 文件管理模块
   └── S3上传实现
   └── 文件访问控制
   └── 生命周期管理

8.6 健康监测模块
   └── DynamoDB数据存储
   └── 时序数据查询
   └── 图表可视化

8.7 Lambda函数
   └── 预约提醒函数
   └── 数据备份函数
   └── EventBridge触发器
```

---

**9. Cloud Services Integration (4-5页)**

```
9.1 AWS EC2部署
   └── 实例配置
   └── 安全组设置
   └── 部署流程
   └── Systemd服务配置

9.2 AWS RDS配置
   └── 数据库实例参数
   └── 备份策略
   └── 性能优化

9.3 AWS S3 + CloudFront
   └── Bucket配置
   └── CDN分发设置
   └── HTTPS配置
   └── 缓存策略

9.4 AWS DynamoDB
   └── 表设计
   └── 读写容量配置
   └── TTL设置

9.5 AWS Lambda
   └── 函数配置
   └── 触发器设置
   └── 日志管理

9.6 成本优化
   └── 免费额度使用情况
   └── 成本控制措施
```

---

**10. Monitoring and Performance (3-4页)**

```
10.1 CloudWatch监控
   └── Dashboard截图
   └── 关键指标说明
   └── 告警配置

10.2 性能分析
   └── API响应时间统计
   └── 数据库查询性能
   └── 前端加载时间

10.3 日志管理
   └── CloudWatch Logs
   └── 错误追踪
   └── 审计日志

10.4 优化措施
   └── 缓存策略
   └── 查询优化
   └── 代码优化
```

---

**11. Testing (3-4页)**

```
11.1 测试策略
   └── 单元测试
   └── 集成测试
   └── 用户验收测试

11.2 测试案例
   └── 功能测试案例表格
   └── 测试结果

11.3 性能测试
   └── 负载测试结果
   └── 压力测试结果

11.4 安全测试
   └── 漏洞扫描
   └── 渗透测试
```

---

**12. User Guide (2-3页)**

```
12.1 患者使用指南
   └── 注册登录
   └── 使用AI助手
   └── 预约挂号
   └── 查看病历

12.2 医生使用指南
   └── 管理预约
   └── 填写病历
   └── 开具处方

12.3 管理员使用指南
   └── 系统监控
   └── 用户管理
```

---

**13. Challenges and Solutions (1-2页)**

```
记录开发过程中遇到的困难和解决方案:
├── 技术挑战
├── AWS配置问题
├── 前后端联调问题
└── 性能优化挑战
```

---

**14. Future Enhancements (1页)**

```
未来改进方向:
├── 移动App开发
├── 实时视频咨询
├── 更多AI功能
├── 医院系统对接
└── 多语言支持
```

---

**15. Conclusion (1页)**

```
总结:
├── 项目成果
├── 技术学习收获
├── 云计算应用价值
└── 个人成长
```

---

**16. References (1-2页)**

```
所有引用的文献和资源:
├── AWS官方文档
├── ASP.NET文档
├── React文档
├── 学术论文
├── 技术博客
└── API文档
```

---

**17. Appendices (附录)**

```
A. 代码片段 (关键代码)
B. API文档 (Swagger导出)
C. 数据库脚本
D. AWS配置文件
E. 测试报告
F. 用户手册
```

---

#### **文档格式要求**:

```
字体:
├── 标题: Arial 14pt, Bold
├── 副标题: Arial 12pt, Bold
├── 正文: Times New Roman 11pt
└── 代码: Consolas 9pt

间距:
├── 行间距: 1.5
├── 段前段后: 6pt
└── 页边距: 上下2.5cm, 左右2cm

图表:
├── 所有图表需有编号和标题
├── 图在标题下方
├── 表在标题上方
└── 高分辨率图片

页眉页脚:
├── 页眉: 项目名称
├── 页脚: 页码
└── 第一页无页眉页脚
```

---

### **Day 7: 准备演示材料**

#### **演示视频 (8-10分钟)**:

**视频结构**:

```
0:00-0:30 开场介绍
├── 自我介绍
├── 项目名称
└── 技术栈概述

0:30-1:30 系统架构演示
├── 架构图讲解
├── AWS服务介绍
└── 前后端分离架构

1:30-3:00 患者端功能演示
├── 注册登录 (15秒)
├── AI症状分析 (30秒)
├── 预约挂号 (30秒)
├── 查看病历 (30秒)
└── 健康数据记录 (15秒)

3:00-4:30 医生端功能演示
├── 查看预约列表 (30秒)
├── 填写病历 (45秒)
├── 开具处方 (45秒)

4:30-5:30 管理员功能演示
├── CloudWatch Dashboard (30秒)
├── 系统监控 (20秒)
└── 区块链验证 (10秒)

5:30-7:00 技术亮点展示
├── AI功能演示 (45秒)
├── 区块链防篡改演示 (30秒)
├── Vector Database搜索 (15秒)

7:00-8:00 AWS服务展示
├── EC2实例 (15秒)
├── RDS数据库 (15秒)
├── S3 + CloudFront (15秒)
└── Lambda函数 (15秒)

8:00-9:00 性能与监控
├── CloudWatch指标 (30秒)
└── 性能测试结果 (30秒)

9:00-10:00 总结
├── 项目成果
├── 技术创新点
└── 学习收获
```

#### **视频制作工具**:

```
屏幕录制: OBS Studio (免费)
视频编辑: DaVinci Resolve (免费版)
字幕: YouTube自动字幕
画外音: 使用麦克风录制
```

#### **视频要求**:

```
✅ 分辨率: 1920x1080 (Full HD)
✅ 帧率: 30 FPS
✅ 格式: MP4 (H.264)
✅ 声音清晰
✅ 操作流畅
✅ 无明显错误
✅ 上传到YouTube (Unlisted)
```

---

#### **PowerPoint演示文稿 (15-20页)**:

**PPT结构**:

```
Slide 1: 封面
├── 项目标题
├── 学生信息
└── 日期

Slide 2: 项目概述
├── 行业背景
└── 解决方案

Slide 3: 系统架构
└── 完整架构图

Slide 4-
```

## **Week 8 (Week 14): 测试、优化与文档** (续)

### **Day 7: 准备演示材料** (续)

#### **PowerPoint演示文稿 (15-20页)** (续):

**PPT结构** (续):

```
Slide 4-5: 技术栈
├── Frontend技术
├── Backend技术
├── 数据库技术
└── AWS云服务

Slide 6: 数据库设计
├── ER图
└── 表关系说明

Slide 7-8: 核心功能展示
├── 用户角色
├── 主要功能列表
└── 功能截图

Slide 9: AI功能亮点
├── 症状分析演示
├── 药物识别
├── Vector Database
└── 实际效果截图

Slide 10: 区块链实现
├── 区块结构
├── 防篡改机制
├── 验证流程
└── 代码片段

Slide 11-12: AWS服务整合
├── EC2部署
├── RDS数据库
├── S3+CloudFront
├── DynamoDB
├── Lambda函数
└── 每个服务的截图

Slide 13: CloudWatch监控
├── Dashboard截图
├── 关键指标
└── 告警配置

Slide 14: 性能优化
├── 优化前后对比
├── 响应时间图表
└── 用户体验提升

Slide 15: 安全措施
├── 认证授权
├── 数据加密
├── AWS安全配置
└── 合规性

Slide 16: 测试结果
├── 功能测试通过率
├── 性能测试数据
└── 用户满意度

Slide 17: 项目成果
├── 完成的功能
├── 技术创新点
├── 满足课业要求
└── Distinction加分项

Slide 18: 挑战与解决方案
├── 遇到的困难
└── 解决方法

Slide 19: 未来展望
├── 功能扩展计划
├── 技术升级方向
└── 商业化潜力

Slide 20: Q&A
└── Thank You
```

#### **PPT设计要求**:

```
设计风格:
├── 专业简洁
├── 统一配色 (蓝色系 - 医疗主题)
├── 每页不超过5个要点
└── 高质量截图和图表

字体:
├── 标题: 24-32pt, Bold
├── 正文: 18-20pt
└── 使用Sans-serif字体 (Arial/Calibri)

视觉元素:
├── 适当使用图标
├── 数据用图表展示
├── 代码用代码框
└── 避免过多文字
```

---

### **Week 8最终检查清单**:

#### **技术功能检查**:

```
前端功能:
✅ 所有页面正常显示
✅ 路由切换正常
✅ API调用成功
✅ 表单验证工作
✅ 错误处理完善
✅ 响应式设计
✅ 无Console错误

后端功能:
✅ 所有API端点正常
✅ 数据库连接稳定
✅ JWT认证工作
✅ 权限控制正确
✅ 错误处理完善
✅ 日志记录完整
✅ 性能符合要求

AWS服务:
✅ EC2正常运行
✅ RDS连接正常
✅ S3文件访问正常
✅ CloudFront分发正常
✅ DynamoDB读写正常
✅ Lambda函数执行成功
✅ CloudWatch监控正常

AI功能:
✅ Gemini API调用成功
✅ 症状分析准确
✅ 药物识别工作
✅ Vector搜索正常

区块链:
✅ 区块创建正常
✅ 链验证成功
✅ 防篡改检测工作
```

#### **文档检查**:

```
✅ 所有章节完整
✅ 图表清晰准确
✅ 页码正确
✅ 目录自动生成
✅ 拼写检查通过
✅ 语法检查通过
✅ 引用格式正确
✅ PDF导出无问题
```

#### **演示材料检查**:

```
✅ 视频录制完成
✅ 视频质量良好
✅ 声音清晰
✅ 已上传YouTube
✅ PPT设计专业
✅ PPT内容完整
✅ 备份所有文件
```

---

## 🎓 **提交清单**

### **最终提交内容**:

#### **1. 文档 (PDF格式)**:

```
主文档:
├── Healthcare_System_Documentation.pdf (30-40页)
├── 包含所有章节
└── 高质量排版

附加文档:
├── User_Manual.pdf (5-10页)
├── API_Documentation.pdf (Swagger导出)
├── Database_Schema.pdf
└── AWS_Configuration_Guide.pdf
```

#### **2. 代码 (GitHub仓库)**:

```
仓库结构:
HealthcareProject/
├── README.md (项目说明)
├── frontend/ (React代码)
├── backend/ (ASP.NET代码)
├── database/ (SQL脚本)
├── docs/ (文档源文件)
└── deployment/ (部署脚本)

README.md包含:
├── 项目介绍
├── 技术栈
├── 本地运行指南
├── 部署指南
├── API文档链接
├── 演示视频链接
└── 截图
```

#### **3. 演示材料**:

```
├── Healthcare_Demo_Video.mp4 (8-10分钟)
├── Healthcare_Presentation.pptx
└── Live_Demo_URL.txt (包含应用访问地址)
```

#### **4. 其他材料**:

```
├── CloudWatch_Dashboard_Export.json
├── Test_Report.pdf
├── Performance_Analysis.pdf
└── Cost_Analysis.xlsx (AWS费用分析)
```

---

## 📊 **评分预估**

### **Task #1: Frontend & Backend Development (30分)**:

```
前端开发 (10分):
├── React架构清晰: 3分
├── UI/UX设计: 2分
├── 响应式设计: 2分
├── 代码质量: 2分
└── 前后端整合: 1分
预估得分: 9-10分

后端开发 (10分):
├── ASP.NET架构: 3分
├── RESTful API设计: 2分
├── 业务逻辑实现: 3分
└── 代码质量: 2分
预估得分: 9-10分

数据库设计 (10分):
├── PostgreSQL设计: 4分
├── 关系设计: 3分
├── 查询优化: 2分
└── 数据完整性: 1分
预估得分: 9-10分

Task #1总分: 27-30分
```

### **Task #2: Cloud Services Integration (20分)**:

```
云服务整合 (10分):
├── EC2部署: 2分
├── RDS配置: 2分
├── S3+CloudFront: 2分
├── DynamoDB: 2分
└── Lambda: 2分
预估得分: 10分

监控工具 (10分):
├── CloudWatch配置: 4分
├── Dashboard创建: 3分
├── 告警设置: 2分
└── 日志管理: 1分
预估得分: 9-10分

Task #2总分: 19-20分
```

### **Distinction加分项 (30+分)**:

```
AI整合 (20分):
├── Gemini API整合: 7分
├── 症状分析功能: 5分
├── 药物识别: 4分
└── pgvector实现: 4分
预估得分: 18-20分

区块链 (15分):
├── 区块链实现: 8分
├── 防篡改验证: 4分
└── 实际应用: 3分
预估得分: 13-15分

NoSQL数据库 (10分):
├── DynamoDB设计: 5分
├── 实际应用: 3分
└── 性能优化: 2分
预估得分: 9-10分

Serverless (10分):
├── Lambda函数: 6分
└── EventBridge: 4分
预估得分: 9-10分

高级架构 (10分):
├── 前后端分离: 3分
├── 微服务设计: 3分
├── 安全架构: 2分
└── 性能优化: 2分
预估得分: 9-10分

加分项总计: 58-65分
```

### **文档与演示 (20分)**:

```
文档质量 (10分):
├── 完整性: 3分
├── 专业性: 3分
├── 图表质量: 2分
└── 技术深度: 2分
预估得分: 9-10分

演示质量 (10分):
├── 视频质量: 3分
├── 功能演示: 4分
└── 讲解清晰: 3分
预估得分: 9-10分

文档演示总分: 18-20分
```

### **最终预估总分**:

```
基础分: 46-50分 (Task #1 + Task #2)
加分项: 58-65分 (AI + Blockchain + 其他)
文档演示: 18-20分

总计: 122-135分 (满分50分计算后)
折算百分比: 85-90%

预估等级: A+ (Distinction) ✅
```

---

## 💰 **完全免费方案 - 费用明细**

### **AWS服务免费额度使用情况**:

```
EC2 (t2.micro):
├── 免费额度: 750小时/月
├── 实际使用: ~720小时/月
├── 费用: $0.00 ✅
└── 注意: 第13个月后需付费或迁移

RDS (db.t3.micro):
├── 免费额度: 750小时/月
├── 实际使用: ~720小时/月
├── 存储: 20GB (免费)
├── 费用: $0.00 ✅
└── 注意: 12个月后需付费

S3:
├── 免费额度: 5GB存储
├── 实际使用: ~2GB
├── GET请求: 20,000次/月 (免费)
├── PUT请求: 2,000次/月 (免费)
├── 费用: $0.00 ✅
└── 超出部分: ~$0.023/GB/月

CloudFront:
├── 免费额度: 1TB数据传输/月
├── 实际使用: ~50GB
├── 费用: $0.00 ✅

DynamoDB:
├── 免费额度: 25GB存储 (永久)
├── 实际使用: ~1GB
├── 读写容量: 按需计费 (免费额度内)
├── 费用: $0.00 ✅

Lambda:
├── 免费额度: 100万次请求/月
├── 实际使用: ~10,000次
├── 计算时间: 400,000 GB-秒/月 (免费)
├── 费用: $0.00 ✅

CloudWatch:
├── 基础监控: 免费
├── 日志: 5GB存储 (免费)
├── Dashboard: 3个免费
├── 告警: 10个免费
├── 费用: $0.00 ✅

数据传输:
├── 入站: 免费
├── 出站: 1GB/月免费
├── 实际使用: ~800MB
├── 费用: $0.00 ✅

预估月费用总计: $0 - $2
项目8周总费用: $0 - $4 ✅
```

### **外部服务费用**:

```
Google Gemini API:
├── 免费额度: 60次/分钟
├── 足够课业使用
├── 费用: $0.00 ✅

GitHub:
├── 免费账户足够
├── 费用: $0.00 ✅

开发工具:
├── Visual Studio Community: 免费
├── VS Code: 免费
├── Node.js: 免费
├── Git: 免费
├── 费用: $0.00 ✅

总计: $0.00 ✅
```

---

## ⚠️ **重要注意事项**

### **AWS费用控制**:

#### **1. 设置预算警报 (必须!)**:

```
AWS Console → Billing → Budgets → Create budget

配置:
├── Budget type: Cost budget
├── Budget amount: $1.00
├── Alert threshold: $0.50 (50%)
├── Email notification: 你的邮箱
└── 立即设置! ⚠️
```

#### **2. 监控使用情况**:

```
每天检查:
├── AWS Billing Dashboard
├── Cost Explorer
└── Free Tier使用情况
```

#### **3. 资源清理**:

```
项目结束后 (课程完成后):
✅ 停止EC2实例
✅ 删除RDS实例
✅ 清空S3 Bucket
✅ 删除CloudFront分发
✅ 删除DynamoDB表
✅ 删除Lambda函数
✅ 删除CloudWatch告警

⚠️ 不立即删除会产生费用!
```

---

### **开发注意事项**:

#### **1. 代码管理**:

```
✅ 每天Git提交
✅ 使用有意义的commit message
✅ 定期push到GitHub
✅ 不要上传敏感信息 (.env文件加入.gitignore)
```

#### **2. 备份策略**:

```
每周备份:
├── 代码推送GitHub
├── 数据库导出SQL
├── 文档保存多个版本
└── AWS配置导出
```

#### **3. 安全最佳实践**:

```
⚠️ 不要在代码中硬编码密钥
⚠️ 不要在GitHub公开敏感信息
⚠️ 定期更换数据库密码
⚠️ 使用环境变量存储配置
```

---

### **时间管理建议**:

#### **每周时间分配**:

```
Week 1: 规划与环境 (15-20小时)
├── 学习AWS基础: 5小时
├── 设置环境: 3小时
├── 创建AWS资源: 4小时
├── 文档准备: 5小时
└── 学习技术栈: 3小时

Week 2: 后端基础 (20-25小时)
├── 创建项目: 3小时
├── 数据库设计: 5小时
├── 认证系统: 8小时
├── 测试调试: 4小时
└── 文档更新: 3小时

Week 3: 后端核心 (20-25小时)
├── 医生管理: 6小时
├── 预约管理: 8小时
├── 病历处方: 6小时
├── 测试: 3小时
└── 文档: 2小时

Week 4: 前端开发 (25-30小时)
├── 项目搭建: 3小时
├── 认证页面: 8小时
├── 核心页面: 12小时
├── 样式美化: 5小时
└── 测试: 2小时

Week 5: AI整合 (20-25小时)
├── Gemini API: 5小时
├── 症状分析: 8小时
├── pgvector: 7小时
├── 前端整合: 3小时
└── 测试: 2小时

Week 6: 高级功能 (20-25小时)
├── 区块链: 10小时
├── S3上传: 4小时
├── DynamoDB: 6小时
└── 测试: 3小时

Week 7: 部署监控 (25-30小时)
├── EC2部署: 8小时
├── S3+CloudFront: 5小时
├── CloudWatch: 7小时
├── Lambda: 5小时
└── 调试优化: 5小时

Week 8: 完善提交 (30-35小时)
├── 功能测试: 8小时
├── 性能优化: 6小时
├── 文档撰写: 12小时
├── 视频制作: 6小时
└── 最终检查: 3小时

总计: 175-215小时 (8周)
平均每天: 3-4小时
周末可多投入时间
```

---

## 🎯 **成功的关键因素**

### **1. 严格遵循计划**:

```
✅ 按周完成任务
✅ 不拖延
✅ 每天进步一点
✅ 遇到问题及时解决
```

### **2. 充分利用资源**:

```
✅ AWS官方文档
✅ Microsoft Learn
✅ Stack Overflow
✅ YouTube教程
✅ ChatGPT/Claude辅助
```

### **3. 持续测试**:

```
✅ 开发一个功能测试一个
✅ 不累积bug
✅ 及时修复问题
```

### **4. 文档同步**:

```
✅ 边做边记录
✅ 截图保存
✅ 不要最后才写文档
```

### **5. 寻求帮助**:

```
✅ 遇到问题先Google
✅ 查看官方文档
✅ 询问讲师
✅ 利用AI助手
```

---

## 📞 **紧急联系信息**

### **讲师咨询**:

```
✅ 每周咨询一次进度
✅ 遇到重大问题及时询问
✅ 确认技术选择是否合理
✅ 提前预约演示时间
```

### **技术支持资源**:

```
AWS Support:
└── 基础计划 (免费)
└── 论坛: https://repost.aws/

ASP.NET Support:
└── Microsoft Q&A
└── .NET Discord

React Support:
└── React Discord
└── Stack Overflow
```

---

## ✅ **最终检查清单**

### **提交前最后确认**:

```
技术实现:
✅ 前端所有功能正常
✅ 后端所有API工作
✅ 数据库连接稳定
✅ AWS服务运行正常
✅ AI功能正常工作
✅ 区块链验证成功
✅ 监控数据正常

文档:
✅ 所有章节完整
✅ 图表清晰
✅ 无拼写错误
✅ 引用格式正确
✅ PDF导出成功

代码:
✅ 已推送GitHub
✅ README完整
✅ 代码注释充分
✅ 无敏感信息

演示:
✅ 视频录制完成
✅ PPT准备完毕
✅ 演示环境测试通过
✅ 备份所有材料

提交:
✅ 所有文件准备完毕
✅ 文件命名规范
✅ 压缩包大小合理
✅ 提交时间充裕
```

---

## 🎉 **总结**

这份计划提供了:

- ✅ **完整的8周开发时间表**
- ✅ **详细的技术栈选择理由**
- ✅ **Step-by-step实施指南**
- ✅ **完全免费的AWS方案**
- ✅ **Distinction级别的加分项**
- ✅ **专业的文档模板**
- ✅ **实用的时间管理建议**

**遵循这个计划,你将能够:**

1. 在8周内完成整个项目
2. 获得Distinction (80%+) 成绩
3. 总费用控制在$0-$5
4. 学到前沿的云计算和AI技术
5. 拥有一个完整的Portfolio项目

**关键成功因素:**

- 每天坚持3-4小时
- 严格按计划执行
- 遇到问题及时解决
- 保持文档同步更新
- 充分测试每个功能

**Good Luck! 你一定可以做到!** 🚀
